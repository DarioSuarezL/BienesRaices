extends ../layouts/index.pug

block content
    div(class="py-10")
        h1(class="text-4xl my-10 font-extrabold text-center") Bienes
            span(class="font-normal") Raices
        h2(class='text-center text-2xl font-extrabold')=title

        if errors
            div(class="max-w-md mx-auto my-10" role="alert")
                each error in errors
                    p(class="bg-red-600 text-white text-xs text-center p-2 mb-1 font-bold")=error.msg

        div(class="mt-8 mx-auto max-w-md")
            div(class="bg-white py-8 px-4 shadow")
                form(class="space-y-5" method="POST")
                    input(type="hidden" name="_csrf" value=csrfToken)

                    div
                        label(class="block text-sm text-gray-500 mb-2 font-bold" for="password") Nueva contraseña
                        input(id="password" name="password" class="w-full px-3 py-2 border border-gray-300 rounded-md placeholder-gray-400" placeholder="Ingresa una nueva contraseña" type="password")
                    
                    input(class="block w-full bg-gray-700 text-white p-2 rounded-md hover:bg-gray-600 font-bold cursor-pointer" type="submit" value="Reestablecer contraseña")